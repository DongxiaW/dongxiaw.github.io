# All page content is edited in _data/data.yml.

# Site Settings
title: Dongxia Wu
description: >
  Dongxia (Allen) Wu — Incoming Assistant Professor at MBZUAI. Research on trustworthy AI
  (calibrated, auditable, physics-grounded models) and AI for science (biomedical, health,
  and spatiotemporal data).
url: 'https://dongxiaw.github.io'
# baseurl left empty: this is a user site served at the domain root

# Sass/SCSS
sass:
  sass_dir: _sass
  style: compressed

encoding: "utf-8"

# Build settings
exclude:
  - README.md
  - Gemfile
  - Gemfile.lock

# Development Settings
port: 4000
host: 0.0.0.0
